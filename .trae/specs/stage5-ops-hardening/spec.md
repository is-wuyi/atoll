# 阶段5：运维加固 — 批次规划总纲

审计（2026-09-12）发现的 12 个问题与原 roadmap（closeout-stage3-and-roadmap）合并为四个批次，按依赖顺序执行。

## 批次总览

| 批次 | 内容 | 状态 |
|---|---|---|
| **A** | 修复扫描鲁棒性（退避/404 探测/上报重试）+ fsync + tmp 清理 + 传输超时 + 优雅退出 | ✅ 完成，提交 `1d3f76f`，4 机实机验证 |
| **B** | 组件间认证（本 spec） | 进行中 |
| **C** | 分片上传（64MB 块，原子覆盖写 #1、并发写 #8、反向孤儿 #4 一并解决） | 待开始 |
| **D** | 运维杂项：日志轮转、mount 自启、节点写入容量限制 (#9) | 待开始 |

## 批次 A 验收记录（实机，2026-09-12）

- 4 机滚动更新至 `1d3f76f`，全部 active
- **inode 114（3GB 模型文件）完成史上首次副本同步**：根因为 node 的 60s 全程 HTTP 超时掐死大对象传输，修复后 6 分钟（~5MB/s）同步完成，done_replicas [9,12] 闭环
- 修复退避日志确认生效（15s→30s→45s→75s）
- master 优雅退出验证：systemd 从 SIGKILL(9) 变为干净 Stopped
- 单测新增：退避倍增/封顶、404 探测、写失败保旧、tmp 清理；全量测试 6 包全绿

## 认证与分片的顺序约定

认证（B）必须在分片（C）之前：分片会新增块级 API，先立认证框架，新接口生来带鉴权。
